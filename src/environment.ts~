declare global {
    namespace NodeJS {
        interface ProcessEnv {
            TIKTOK_CLIENT_ID: string,
            TIKTOK_CLIENT_SECRET: string,
            NODE_ENV: 'dev' | 'prod';
        }
    }
}

export {}
